{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Chat Notebook article frontmatter",
  "description": "Required metadata for one self-contained article. Additional MyST fields remain supported.",
  "type": "object",
  "required": ["title", "description", "date"],
  "properties": {
    "title": { "type": "string", "minLength": 1, "pattern": "\\S" },
    "description": { "type": "string", "minLength": 1, "pattern": "\\S" },
    "date": { "type": "string", "format": "date", "description": "Original creation date, YYYY-MM-DD; keep stable on revision." }
  },
  "additionalProperties": true
}
